//archivo que contiene un array de frutas pero se exporta para ser ejecutado desde el archivo ejemplo-uno
// exportacion de multiples valores

const frutas = ['platano', 'manzana', 'platano','pera'];
const dinero = 1000;

module.exports = {
    frutas,
    dinero,
};

